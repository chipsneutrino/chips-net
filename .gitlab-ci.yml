image: "registry.gitlab.com/chipsneutrino/chips-net/chips-net:latest"

stages:
  - build
  - test
  - pages

build:
  stage: build
  image: docker:19.03.1
  services:
    - docker:19.03.1-dind
  variables:
    DOCKER_HOST: tcp://docker:2376
    DOCKER_TLS_CERTDIR: "/certs"
  script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
    - docker build -t $CI_REGISTRY/chipsneutrino/chips-net/chips-net:latest .
    - docker push $CI_REGISTRY/chipsneutrino/chips-net/chips-net:latest
  only:
    refs:
      - master
    changes:
      - environment.yaml
      - Dockerfile

flake8:
  stage: test
  script:
    - source /opt/conda/bin/activate
    - source activate chipsnet
    - flake8 --max-line-length=120 chipsnet/*.py

pytest:
  stage: test
  script:
    - source /opt/conda/bin/activate
    - source activate chipsnet
    - export PYTHONPATH=$PYTHONPATH:$(pwd)
    - pytest

pages:
  stage: pages
  script:
    - source /opt/conda/bin/activate
    - source activate chipsnet
    - pdoc --html chipsnet -o ./doc --force
    - mkdir .public
    - cp -r ./doc/chipsnet/* .public
    - mv .public public
  artifacts:
    paths:
      - public
  environment:
    name: production/pages
    url: https://chipsneutrino.gitlab.io/chips-net
  only:
    - master
    
